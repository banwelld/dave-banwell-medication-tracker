import React from "react";
import NavBar from "./NavBar";

function Header() {
    return (
        <header>
            <h1>My Medication Tracker</h1>
            <NavBar />
        </header>
    );
};

export default Header;